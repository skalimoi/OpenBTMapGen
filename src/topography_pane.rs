use fltk::input::Input;
use fltk::prelude::{InputExt, WidgetExt};
use ui::UserInterface;
use crate::topo_settings::TopoSettings;
use crate::ui;

pub fn seed_input_callback(b: &mut Input, topo_settings: &mut TopoSettings) {

}